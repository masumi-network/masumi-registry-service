// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider          = "postgresql"
  url               = env("DATABASE_URL")
  shadowDatabaseUrl = env("SHADOW_DATABASE_URL")
}

model ApiKey {
  id                      String       @id @default(cuid())
  createdAt               DateTime     @default(now())
  updatedAt               DateTime     @updatedAt
  tokenHash               String       @unique
  status                  APIKeyStatus
  permission              Permission
  usageLimited            Boolean      @default(false)
  accumulatedUsageCredits Float        @default(0)
  maxUsageCredits         Float?
  Usage                   UsageEntry[]
}

enum APIKeyStatus {
  Active
  Revoked
}

enum Permission {
  User
  Admin
}

model UsageEntry {
  id          String   @id @default(cuid())
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  usedCredits Float
  note        String?

  ApiKey   ApiKey? @relation(fields: [apiKeyId], references: [id])
  apiKeyId String?
}

model RegistryEntry {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  name               String
  apiBaseUrl         String
  description        String?
  authorName         String?
  authorContactEmail String?
  authorContactOther String?
  authorOrganization String?

  privacyPolicy     String?
  termsAndCondition String?
  otherLegal        String?
  image             String?
  tags              String[]

  agentCardUrl        String?
  a2aProtocolVersions String[]

  a2aAgentVersion       String?
  a2aDefaultInputModes  String[]
  a2aDefaultOutputModes String[]
  a2aProviderName       String?
  a2aProviderUrl        String?
  a2aDocumentationUrl   String?
  a2aIconUrl            String?

  lastUptimeCheck  DateTime
  uptimeCount      Int            @default(0)
  uptimeCheckCount Int            @default(0)
  status           Status
  statusUpdatedAt  DateTime       @default(now())
  RegistrySource   RegistrySource @relation(fields: [registrySourceId], references: [id], onDelete: Cascade)

  paymentType    PaymentType @default(Web3CardanoV1)
  Capability     Capability? @relation(fields: [capabilitiesId], references: [id])
  capabilitiesId String?

  registrySourceId String

  assetIdentifier String       @unique
  searchText      String       @default("")
  AgentPricing    AgentPricing @relation(fields: [agentPricingId], references: [id])
  agentPricingId  String

  ExampleOutput          ExampleOutput[]
  A2ASkills              A2ASkill[]
  A2ASupportedInterfaces A2ASupportedInterface[]
  A2ACapabilities        A2ACapabilities?

  metadataVersion Int

  @@index([statusUpdatedAt])
  @@index([statusUpdatedAt, id])
}

model InboxAgentRegistration {
  id              String                       @id @default(cuid())
  createdAt       DateTime                     @default(now())
  updatedAt       DateTime                     @updatedAt
  status          InboxAgentRegistrationStatus @default(Pending)
  statusUpdatedAt DateTime                     @default(now())

  name                 String
  description          String?
  agentSlug            String
  providerUrl          String?
  assetIdentifier      String  @unique
  linkedEmail          String?
  encryptionPublicKey  String?
  encryptionKeyVersion String?
  signingPublicKey     String?
  signingKeyVersion    String?
  metadataVersion      Int

  RegistrySource   RegistrySource @relation(fields: [registrySourceId], references: [id], onDelete: Cascade)
  registrySourceId String

  @@index([name])
  @@index([agentSlug])
  @@index([linkedEmail])
  @@index([status])
  @@index([registrySourceId])
  @@index([registrySourceId, status, updatedAt])
  @@index([statusUpdatedAt])
  @@index([statusUpdatedAt, id])
}

model ExampleOutput {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name      String
  mimeType  String
  url       String

  RegistryEntry   RegistryEntry? @relation(fields: [registryEntryId], references: [id])
  registryEntryId String?
}

model AgentPricing {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  pricingType         PricingType
  FixedPricing        AgentFixedPricing? @relation(fields: [agentFixedPricingId], references: [id])
  agentFixedPricingId String?            @unique
  RegistryEntry       RegistryEntry[]
}

enum PricingType {
  Fixed
  Free
  Dynamic
}

model AgentFixedPricing {
  id           String        @id @default(cuid())
  createdAt    DateTime      @default(now())
  updatedAt    DateTime      @updatedAt
  AgentPricing AgentPricing?
  Amounts      UnitValue[]
}

model UnitValue {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  amount              BigInt
  unit                String
  AgentFixedPricing   AgentFixedPricing? @relation(fields: [agentFixedPricingId], references: [id])
  agentFixedPricingId String?
}

enum PaymentType {
  Web3CardanoV1
  None
}

enum Status {
  Online
  Offline
  Deregistered
  Invalid
}

enum InboxAgentRegistrationStatus {
  Pending
  Verified
  Invalid
  Deregistered
}

model Capability {
  id            String          @id @default(cuid())
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  name          String
  version       String
  description   String?
  RegistryEntry RegistryEntry[]

  @@unique([name, version])
}

model RegistrySource {
  id                     String               @id @default(cuid())
  createdAt              DateTime             @default(now())
  updatedAt              DateTime             @updatedAt
  network                Network
  url                    String?
  policyId               String
  RegistrySourceConfig   RegistrySourceConfig @relation(fields: [registrySourceConfigId], references: [id])
  registrySourceConfigId String

  note                   String?
  lastTxId               String?
  lastCheckedPage        Int                      @default(1) //blockfrost starts at page 1
  RegistryEntry          RegistryEntry[]
  InboxAgentRegistration InboxAgentRegistration[]

  @@unique([network, policyId])
}

enum RPCProvider {
  Blockfrost
}

model RegistrySourceConfig {
  id                String           @id @default(cuid())
  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  rpcProvider       RPCProvider
  rpcProviderApiKey String
  RegistrySource    RegistrySource[]
}

enum Network {
  Preprod
  Mainnet
}

model UpdatedRegistryEntriesLog {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model A2ASkill {
  id              String        @id @default(cuid())
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  skillId         String
  name            String
  description     String
  tags            String[]
  examples        String[]
  inputModes      String[]
  outputModes     String[]
  RegistryEntry   RegistryEntry @relation(fields: [registryEntryId], references: [id], onDelete: Cascade)
  registryEntryId String
}

model A2ASupportedInterface {
  id              String        @id @default(cuid())
  createdAt       DateTime      @default(now())
  updatedAt       DateTime      @updatedAt
  url             String
  protocolBinding String
  protocolVersion String
  tenant          String?
  RegistryEntry   RegistryEntry @relation(fields: [registryEntryId], references: [id], onDelete: Cascade)
  registryEntryId String
}

model A2ACapabilities {
  id                String        @id @default(cuid())
  createdAt         DateTime      @default(now())
  updatedAt         DateTime      @updatedAt
  streaming         Boolean?
  pushNotifications Boolean?
  extendedAgentCard Boolean?
  extensions        Json?
  RegistryEntry     RegistryEntry @relation(fields: [registryEntryId], references: [id], onDelete: Cascade)
  registryEntryId   String        @unique
}
