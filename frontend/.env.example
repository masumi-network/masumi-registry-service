# Used only for `pnpm run frontend:dev` (Next on :3001 talking to the API on :3000).
# Production build (served at /admin by the registry) defaults to same-origin `/api/v1`.
NEXT_PUBLIC_REGISTRY_API_BASE_URL=http://localhost:3000/api/v1
